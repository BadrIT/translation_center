%a.category{ href: category_path(category) }
  .category_name
    = category.view_name
  %span
    = category.complete_percentage_in(to_lang)
    = '%'
    = t('.Complete')
    .progress.progress-info
      .bar{:style => "width: #{category.complete_percentage_in(to_lang)}%"}